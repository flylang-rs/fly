[package]
name = "flylang-diagnostics"
version = "0.1.0"
edition = "2024"

[dependencies]
flylang-common = { path = "../common" }
flylang-parser = { path = "../parser" }
owo-colors = { version = "4.3.0", features = ["supports-colors"] }

